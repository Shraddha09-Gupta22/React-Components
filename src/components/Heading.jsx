import React from "react"; //

function Heading() {
  return <h1>My Favourite Foods</h1>;
}

export default Heading; // exporting the heading component module to the react module....
// cannot use Heading() because it return the value but we don't want to do it so.
